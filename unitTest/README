
Create local_settings.m to suit your testing environments.

example contents of local_settings.m:

%%%
base_dir = 'some directory';
MATLAB_IMPL_DIR = 'the path point to matlab implementation, including the twoLayerFF.m, twoLayerBP.m and so on';
%%%
